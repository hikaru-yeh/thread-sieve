# Copy to .env and fill in.

# Non-secret paths live in config.json under "paths".

# (Optional) Path to threads-scriber.user.js source — only needed when running _rebuild_userscript.py.
THREADS_SCRIBER_PATH=..\threads-scriber\threads-scriber.user.js

# Gemini API key for classify_to_scribe_ai.py.
# Also required for image OCR when config.json has image-ocr.backend=gemini.
GEMINI_API_KEY=

# Gemini model used for classification.
CLASSIFIER_MODEL=gemini-2.5-flash

# Image OCR step toggle. OCR backend/method/category settings live in config.json.
IMAGE_OCR_ENABLED=true
IMAGE_OCR_MODEL=gemini-2.5-flash

# Chandra OCR vLLM settings.
# Used when config.json has image-ocr.backend=chandra and image-ocr.method=vllm.
# These public defaults come from https://github.com/datalab-to/chandra.
# Check that repo for the latest Chandra configuration.
MODEL_CHECKPOINT=datalab-to/chandra-ocr-2
MAX_OUTPUT_TOKENS=12384
VLLM_API_BASE=http://localhost:8000/v1
VLLM_MODEL_NAME=chandra
VLLM_GPUS=0

# Watcher tuning.
DEBOUNCE_SECONDS=2.0
POLL_SECONDS=1.0
